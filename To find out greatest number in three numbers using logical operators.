To find out greatest number in three numbers using logical operators.
#include<stdio.h>
void main()
{
int a,b,c,greatest;
printf("enter three numbers");
scanf("%d%d%d",&a,&b,&c);

if(a>b && a>c)
{printf("\n %d is the greatest number",a);}

else
{
if (b>c)
{printf("\n %d is the greatest number",b);}
else
{printf("\n %d is greatest number",c);}
}

}
