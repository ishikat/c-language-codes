To find out greatest number in three numbers using nest it if.
#include<stdio.h>
void main()
{
int a,b,c;
printf("enter any number");
scanf("%d%d%d",&a,&b,&c);

if (a>b)
{
if (a>c)
{printf("\na is the greatest number");}
else 
{printf("\nc is the greatest number");}
}
else
{
if (b>c)
{printf("\nb is the greatest number");}
else
printf("\nc is the greatest number");}
}
}
